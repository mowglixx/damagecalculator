# NextJS Version of damagecalculator

```js
// install yarn
npm i -g yarn
// run package
yarn && yarn dev
```

```js
scipts: {
    dev: "next dev",
    build: "next build",
    start: "next start",
    docker: "docker-compose up -d",
    dockerAttach: "docker-compose up"
    }
```
